# Slack Token - use ONE of these options:

# Option 1: Bot Token (from OAuth & Permissions > Bot User OAuth Token)
# SLACK_TOKEN=xoxb-your-bot-token

# Option 2: User Token (from OAuth & Permissions > User OAuth Token)
# SLACK_TOKEN=xoxp-your-user-token

# Option 3: Rotatable User Token (expires in 12h, auto-refreshes)
# SLACK_TOKEN=xoxe.xoxp-your-rotatable-token
# SLACK_REFRESH_TOKEN=xoxe-your-refresh-token
